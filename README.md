# phoxal.com

Static public website for Phoxal Framework.

- `index.html` — product page
- `styles.css` and `assets/` — presentation
- `install.sh` — public installer

The site is intentionally small while the pre-1.0 product and implementation
continue to evolve.
